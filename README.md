# Diabetics_detection_system
This project is a machine learning–based diabetes prediction system that predicts the likelihood of diabetes using clinical and lifestyle data. Multiple ML models were trained and evaluated, including Logistic Regression, Random Forest, Decision tree, SVM, KNN and Neural Network (MLP).
